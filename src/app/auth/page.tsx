import React from "react";
import Login from "./Login";

export default function Auth() {
	// they have to be a way to check if a user is logged in
	// if not loggedIn, show the login page
	// if user doesn't have an account instead register
	// if user forgotPassword

	return <Login />;
}
